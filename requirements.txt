# Core dependencies for Streamlit Portuguese Pronunciation Trainer
streamlit>=1.50.0
openai-whisper>=20250625
gtts>=2.5.4
soundfile>=0.13.1
numpy>=2.0.2

# Note: espeak-ng needs to be installed system-wide on the deployment server
# For local development: ./configure && make && make install
