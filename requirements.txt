# Core dependencies for Streamlit Portuguese Pronunciation Trainer
streamlit>=1.39.0
openai-whisper>=20231117
gtts>=2.5.4
soundfile>=0.13.1
numpy>=2.0.2

# wav2vec2 Portuguese ASR engine (alternative to Whisper)
transformers>=4.30.0
torch>=2.0.0
librosa>=0.10.0

# Note: espeak-ng needs to be installed system-wide on the deployment server
# For local development: ./configure && make && make install

# Multi-user authentication and database
mysql-connector-python==9.4.0
argon2-cffi==25.1.0
paramiko<3.0
sshtunnel>=0.4.0
